# Copy to .env.local; the application must explicitly load .env.local.
# Never commit real credentials. These blank values are intentional.
OPENAI_API_KEY=
OPENAI_PROJECT_ID=
OPENAI_MODEL=gpt-5.6-luna
LANGSMITH_API_KEY=
LANGSMITH_WORKSPACE_ID=
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_PROJECT=sp-solution-test-assignment
LANGSMITH_DATASET_NAME=sp-solution-acceptance-v1
LANGSMITH_TRACING=false
AGENT_BUDGET_USD=5
AGENT_ARTIFACT_DIR=artifacts
# Synthetic evaluations export explicitly with --langsmith; live tracing is disabled.
